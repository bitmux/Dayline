node_modules/
.venv/
__pycache__/
.pytest_cache/

# Build output. The bundle that ships is the staged copy inside the integration
# (custom_components/day_spine/www/), because HACS installs only what is
# committed — an ignored bundle means an installed integration with no card.
dist/
dev/dev-bundle.js
dev/dev-bundle.js.map
