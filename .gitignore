node_modules/
.venv/
__pycache__/
.pytest_cache/

# Build output. Nothing ships from here: `npm run stage-card` copies the bundle
# into the sibling Dayline-card checkout, which is the HACS Dashboard repository
# and is where the committed, installed copy lives.
dist/
dev/dev-bundle.js
dev/dev-bundle.js.map

# a long-lived access token lives here
.ha-env

# built for hand-copying to an instance; regenerate with tools/package.sh
dayline-integration.tar.gz

# generated from a live instance — could hold real calendar entries
dev/live-sample.json
